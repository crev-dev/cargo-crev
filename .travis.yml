os:
  - windows
  - linux
  - osx

language: rust
rust:
  - stable
  - beta
  - nightly

script:
  - cargo build --verbose
  - cargo test --verbose
  - if [ "$TRAVIS_RUST_VERSION" == "stable" ]; rustup component add rustfmt && cargo fmt --all -- --check; fi
